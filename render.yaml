services:
  - type: web
    name: scuta-chat
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SCUTA_ALLOWED_ORIGINS
        sync: false
